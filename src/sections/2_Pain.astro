---
// import SocialVisual from '../assets/socialVisual.astro';

type splitSection = {
   heading: string,
   subheading?: Array<string>,
   body: string
}
interface Props {
   data: {
      visual: splitSection,
      copy: splitSection
   };
}

const { data } = Astro.props as Props;
---
<section id="pain" class="section">
   <div class="container grid">
      <div class="copy | flow">
         <h2>{data.copy.heading}</h2>
         <h3>{data.copy.subheading?.map((sub) => (
            <span>{sub}</span>))}</h3>
         <p>{data.copy.body}</p>
      </div>
      <div class="visual | stack">
         <h3>{data.visual.heading}</h3>
         <div class="socialVisual placeholder">
            <!-- <SocialVisual /> -->
         </div>
         <p>{data.visual.body}</p>
      </div>
   </div>
</section>

<style>
   .section {
      container-type: inline-size;
   }
   .copy span {
      display: block;
      padding-block-end: var(--size-4);
   }
   .visual {
      text-align: center;
   }

   @container (max-width: 40rem) {
      .copy {
         text-align: center;
      }
   }
</style>